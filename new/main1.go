package main

import (
	"fmt"
	"time"
)

func NextWorkday(start time.Time) time.Time {
	a := 243
	err.Error()
}

func main() {
	pastTime := time.Now().Add(-2 * time.Hour)
	timeAgo := TimeAgo(pastTime)
	fmt.Println(timeAgo)
}
